achievements:
  kafka:
    listener:
      topic: ${LISTENER_TOPIC_NAME}

spring:
  application:
    name: notification-service

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}
    consumer:
      group-id: ${CONSUMER_GROUP_ID}
